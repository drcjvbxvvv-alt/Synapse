apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synapse.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "synapse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  containers:
  - name: wget
    image: registry.cn-hangzhou.aliyuncs.com/clay-wangzhi/busybox:1.36
    command: ['wget']
    args: ['{{ include "synapse.fullname" . }}-backend:{{ .Values.backend.service.port }}/healthz']
  restartPolicy: Never
