🎉 Synapse 已成功安装！

访问信息:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
  使用 kubectl port-forward 访问:
  
  前端: kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "synapse.fullname" . }}-frontend 8080:80
  访问: http://localhost:8080
  
  后端: kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "synapse.fullname" . }}-backend 8081:8080
  API: http://localhost:8081
{{- end }}

默认登录信息:
  用户名: {{ .Values.init.admin.username }}
  密码: {{ .Values.init.admin.password }}
  
⚠️  首次登录后请立即修改默认密码！

查看服务状态:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

查看日志:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=backend --tail=100 -f

获取帮助:
  文档: https://synapse.clay-wangzhi.com/docs
  GitHub: https://github.com/clay-wangzhi/Synapse

{{ if not .Values.security.jwtSecret }}
ℹ️  JWT Secret 已自动生成（32 位随机字符串）
  升级时会自动复用现有值，无需手动设置
{{- end }}

{{ if and .Values.mysql.internal.enabled (not .Values.mysql.internal.rootPassword) (not .Values.mysql.internal.existingSecret) }}
⚠️  警告: MySQL 密码未设置！
  建议重新安装并设置密码:
  helm upgrade {{ .Release.Name }} synapse/synapse \
    --set mysql.internal.rootPassword="your-mysql-root-password" \
    --set mysql.internal.password="your-mysql-password" \
    -n {{ .Release.Namespace }}
{{- end }}
